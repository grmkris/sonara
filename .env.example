# apps/server
FAL_KEY=
# Optional — enables LLM drift synthesis (Claude Haiku 4.5). Without it the
# server still runs and falls back to raw voice phrases or the static pool.
ANTHROPIC_API_KEY=
PORT=3001
LOG_LEVEL=info

# apps/web
NEXT_PUBLIC_WS_URL=ws://localhost:3001/ws
